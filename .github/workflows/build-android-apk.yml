name: Build IRONWAKE Android APK
on:
  push:
    branches:
      - graphics-overhaul-2026-09
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Verify runner
        run: |
          set -eux
          echo "IRONWAKE APK build runner OK"
          git rev-parse HEAD
