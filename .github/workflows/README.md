# CI

| Workflow | What |
|----------|------|
| `build-legacy-apk.yml` | Debug APK from `legacy-webview/` (Kotlin WebView) |

**Unity APK** (`unity/Ironwake/`) is built **locally** in the Unity Editor for now.
GameCI / Unity license on GitHub Actions is not configured.
